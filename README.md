# Mandi Bagley — Content Creator Portfolio

Single-page static portfolio. No build step, no dependencies.

- `index.html` — the entire site (inline CSS + JS)
- `vercel.json` — static hosting config

## Local preview

```bash
open index.html
```

## Deploy

Static site — Vercel serves `index.html` from the repo root with no framework preset.
